import React from 'react';
import Layout from '../Layout/Layout';

const SessionError: React.FC<any> = () => {
    return (
        <Layout>
            <div>SESSION IS NOT FOUND</div>
        </Layout>
    );
};

export default SessionError;
